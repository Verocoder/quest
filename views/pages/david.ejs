<!DOCTYPE html>
<html>
  <head>
    <% include ../partials/header.ejs %>
  </head>
  <body>
  <% include ../partials/nav.ejs %>

    <div class="well" id="mapid"></div>
    <script>
      var map = new Mapping("mapid","listid");

      function onMapMove(e) {
        map.requestDiscoveries(map.map.getCenter());
      };
      //listeners
      map.map.on('moveend', onMapMove);
    </script>

    <div class = "well" id="listid">
      gonna have some content
    </div>
    <p>under the map</p>

  </body>
</html>
